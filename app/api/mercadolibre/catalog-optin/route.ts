/**
 * @deprecated Canonical endpoint is /api/ml/catalog-optin (comprehensive with bulk support).
 * Thin re-export kept for backward compatibility.
 */
export { GET, POST } from "@/app/api/ml/catalog-optin/route"
